import 'package:flutter/material.dart';

class BookmarkRecipe extends StatelessWidget {
  const BookmarkRecipe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
